---
import { useTranslations } from '../../i18n/utils';

interface Props {
  lang: string;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<div class="bg-gradient-to-r from-purple-600/20 to-blue-600/20 backdrop-blur-sm border border-purple-400/30 rounded-xl p-6 mb-8">
  <div class="flex items-center mb-4">
    <div class="w-12 h-12 bg-purple-500/30 rounded-full flex items-center justify-center mr-4">
      <span class="text-2xl">🧪</span>
    </div>
    <div>
      <h3 class="text-xl font-bold text-white">
        {lang === 'es' ? 'Programa de Testers Beta' : 'Beta Testing Program'}
      </h3>
      <p class="text-purple-200 text-sm">
        {lang === 'es' ? 'Ayúdanos a perfeccionar el curso' : 'Help us perfect the course'}
      </p>
    </div>
  </div>
  
  <p class="text-white/90 mb-6 leading-relaxed">
    {lang === 'es' 
      ? 'Como tester beta, serás parte fundamental del desarrollo de este curso gratuito sobre blockchain y ciencia descentralizada (DeSci) usando Cardano. Tu feedback nos ayudará a crear la mejor experiencia educativa posible.'
      : 'As a beta tester, you\'ll be a fundamental part of developing this free course on blockchain and decentralized science (DeSci) using Cardano. Your feedback will help us create the best possible educational experience.'
    }
  </p>
  
  <div class="grid md:grid-cols-3 gap-4">
    <div class="bg-white/10 rounded-lg p-4 text-center">
      <div class="text-3xl mb-2">⚡</div>
      <h4 class="font-semibold text-white mb-1">
        {lang === 'es' ? 'Acceso Temprano' : 'Early Access'}
      </h4>
      <p class="text-sm text-purple-200">
        {lang === 'es' ? 'Sé el primero en explorar el contenido' : 'Be the first to explore the content'}
      </p>
    </div>
    
    <div class="bg-white/10 rounded-lg p-4 text-center">
      <div class="text-3xl mb-2">💰</div>
      <h4 class="font-semibold text-white mb-1">50 ADA</h4>
      <p class="text-sm text-purple-200">
        {lang === 'es' ? 'Compensación por tu tiempo' : 'Compensation for your time'}
      </p>
    </div>
    
    <div class="bg-white/10 rounded-lg p-4 text-center">
      <div class="text-3xl mb-2">🏆</div>
      <h4 class="font-semibold text-white mb-1">NFT</h4>
      <p class="text-sm text-purple-200">
        {lang === 'es' ? 'Certificado digital exclusivo' : 'Exclusive digital certificate'}
      </p>
    </div>
  </div>
</div>
